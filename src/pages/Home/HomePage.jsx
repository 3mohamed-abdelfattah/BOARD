import React, { Fragment } from 'react'
import { Header, HeroSection } from '@/components'

export const HomePage = () => {

    const styles = {
        mainContainer: ``
    }

    return (
        <Fragment>
            <Header />
            <HeroSection />
        </Fragment>
    )
}